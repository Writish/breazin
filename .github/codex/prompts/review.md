# Independent read-only review

Review the diff without modifying the repository. Prioritize project corruption, compatibility regressions, credentials and privacy leaks, unsafe agent/MCP file or network access, environment cross-talk, provider idempotency, release/update integrity, and missing tests.

Do not approve based on prose alone. Tie every finding to code and distinguish new failures from the pinned upstream baseline.
